declare module '@mui/material/styles' {
  export interface Theme {
    palette: {
      primary: {
        light?: string;
        main: string;
        dark?: string;
        contrastText?: string;
      }
    }
  }

  export interface ThemeOptions {
    palette: {
      primary: {
        light?: string;
        main: string;
        dark?: string;
        contrastText?: string;
      }
    }
  }
}
